#!/bin/bash

usage() {
    echo >&2 "usage: $0 [-d database] select statement (without the select keyword ...)"
    exit 1
}

: ${DBNAME:=mysql}
while getopts :d: ch; do
    case "$ch" in
      d) DBNAME="$OPTARG";;
      *) usage;;
    esac
done

mysql -D $DBNAME -B -t <<END
  select $*;
END

